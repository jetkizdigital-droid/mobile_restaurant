name: Restaurant iOS CI

on:
  workflow_dispatch:
  push:
    branches:
      - ios/restaurant-initial-release
    paths:
      - 'lib/**'
      - 'test/**'
      - 'ios/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'tool/prepare_ios_app_icon.swift'
      - '.github/workflows/restaurant-ios-ci.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'lib/**'
      - 'test/**'
      - 'ios/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'tool/prepare_ios_app_icon.swift'
      - '.github/workflows/restaurant-ios-ci.yml'

permissions:
  contents: read

concurrency:
  group: restaurant-ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios-simulator:
    runs-on: macos-26
    timeout-minutes: 50

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Environment
        shell: bash
        run: |
          set -euo pipefail
          flutter --version
          xcodebuild -version
          pod --version
          xcrun simctl list runtimes

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Tests
        run: flutter test

      - name: Generate branded iOS app icons
        shell: bash
        run: |
          set -euo pipefail
          BRAND_SRC=assets/branding/jetkiz_wordmark.png
          SRC="$RUNNER_TEMP/jetkiz-restaurant-ios-app-icon.png"
          DIR=ios/Runner/Assets.xcassets/AppIcon.appiconset

          test -s "$BRAND_SRC"
          swift tool/prepare_ios_app_icon.swift "$BRAND_SRC" "$SRC"

          sips -g pixelWidth "$SRC" | grep -F 'pixelWidth: 1024'
          sips -g pixelHeight "$SRC" | grep -F 'pixelHeight: 1024'
          sips -g hasAlpha "$SRC" | grep -F 'hasAlpha: no'

          while IFS=: read -r file size; do
            sips -z "$size" "$size" "$SRC" --out "$DIR/$file" >/dev/null
          done <<'EOF'
          Icon-App-20x20@1x.png:20
          Icon-App-20x20@2x.png:40
          Icon-App-20x20@3x.png:60
          Icon-App-29x29@1x.png:29
          Icon-App-29x29@2x.png:58
          Icon-App-29x29@3x.png:87
          Icon-App-40x40@1x.png:40
          Icon-App-40x40@2x.png:80
          Icon-App-40x40@3x.png:120
          Icon-App-60x60@2x.png:120
          Icon-App-60x60@3x.png:180
          Icon-App-76x76@1x.png:76
          Icon-App-76x76@2x.png:152
          Icon-App-83.5x83.5@2x.png:167
          Icon-App-1024x1024@1x.png:1024
          EOF

          test -s "$DIR/Icon-App-1024x1024@1x.png"
          echo 'Opaque JETKIZ Restaurant AppIcon set generated'

      - name: Validate Restaurant iOS configuration
        shell: bash
        run: |
          set -euo pipefail

          grep -F 'PRODUCT_BUNDLE_IDENTIFIER = asia.jetkiz.restaurant;' ios/Runner.xcodeproj/project.pbxproj
          grep -F 'CODE_SIGN_ENTITLEMENTS = Runner/Runner.Debug.entitlements;' ios/Runner.xcodeproj/project.pbxproj
          grep -F 'CODE_SIGN_ENTITLEMENTS = Runner/Runner.Release.entitlements;' ios/Runner.xcodeproj/project.pbxproj
          grep -F 'com.apple.Push' ios/Runner.xcodeproj/project.pbxproj
          grep -F 'com.apple.BackgroundModes' ios/Runner.xcodeproj/project.pbxproj

          /usr/libexec/PlistBuddy -c 'Print :aps-environment' ios/Runner/Runner.Debug.entitlements | grep -Fx 'development'
          /usr/libexec/PlistBuddy -c 'Print :aps-environment' ios/Runner/Runner.Release.entitlements | grep -Fx 'production'
          /usr/libexec/PlistBuddy -c 'Print :UIBackgroundModes' ios/Runner/Info.plist | grep -F 'remote-notification'
          test -n "$(/usr/libexec/PlistBuddy -c 'Print :NSPhotoLibraryUsageDescription' ios/Runner/Info.plist)"
          test -n "$(/usr/libexec/PlistBuddy -c 'Print :NSCameraUsageDescription' ios/Runner/Info.plist)"

          test -s ios/Runner/GoogleService-Info.plist
          /usr/libexec/PlistBuddy -c 'Print :BUNDLE_ID' ios/Runner/GoogleService-Info.plist | grep -Fx 'asia.jetkiz.restaurant'
          /usr/libexec/PlistBuddy -c 'Print :PROJECT_ID' ios/Runner/GoogleService-Info.plist | grep -Fx 'jetkiz-mobile'
          /usr/libexec/PlistBuddy -c 'Print :GOOGLE_APP_ID' ios/Runner/GoogleService-Info.plist | grep -Fx '1:97147344656:ios:0719e155ebe825205f7795'
          grep -F 'GoogleService-Info.plist in Resources' ios/Runner.xcodeproj/project.pbxproj
          echo 'Firebase iOS configuration verified'

      - name: Build iPhone Simulator
        run: flutter build ios --simulator --debug

      - name: Verify simulator bundle
        shell: bash
        run: |
          set -euo pipefail
          APP=build/ios/iphonesimulator/Runner.app
          test -d "$APP"
          test -f "$APP/Info.plist"
          /usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$APP/Info.plist" | grep -Fx 'asia.jetkiz.restaurant'
          /usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "$APP/Info.plist" | grep -Fx 'JETKIZ Ресторан'
          test -s "$APP/GoogleService-Info.plist"
          /usr/libexec/PlistBuddy -c 'Print :BUNDLE_ID' "$APP/GoogleService-Info.plist" | grep -Fx 'asia.jetkiz.restaurant'
          /usr/libexec/PlistBuddy -c 'Print :PROJECT_ID' "$APP/GoogleService-Info.plist" | grep -Fx 'jetkiz-mobile'
          /usr/libexec/PlistBuddy -c 'Print :GOOGLE_APP_ID' "$APP/GoogleService-Info.plist" | grep -Fx '1:97147344656:ios:0719e155ebe825205f7795'
          echo 'Restaurant Firebase plist is present in simulator app bundle'
          echo 'Restaurant iOS simulator bundle verified'

      - name: Build unsigned iPhone release
        run: flutter build ios --release --no-codesign

      - name: Verify unsigned iPhone release
        shell: bash
        run: |
          set -euo pipefail
          APP=build/ios/iphoneos/Runner.app
          test -d "$APP"
          test -f "$APP/Info.plist"
          /usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$APP/Info.plist" | grep -Fx 'asia.jetkiz.restaurant'
          /usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "$APP/Info.plist" | grep -Fx 'JETKIZ Ресторан'
          test -s "$APP/GoogleService-Info.plist"
          /usr/libexec/PlistBuddy -c 'Print :BUNDLE_ID' "$APP/GoogleService-Info.plist" | grep -Fx 'asia.jetkiz.restaurant'
          /usr/libexec/PlistBuddy -c 'Print :PROJECT_ID' "$APP/GoogleService-Info.plist" | grep -Fx 'jetkiz-mobile'
          file "$APP/Runner" | grep -E 'arm64|Mach-O'
          du -sh "$APP"
          echo 'Unsigned Restaurant release for physical iPhone verified'

      - name: Verify packaged privacy manifests
        shell: bash
        run: |
          set -euo pipefail
          APP=build/ios/iphoneos/Runner.app
          PRIVACY_MANIFESTS="$(find "$APP" -name PrivacyInfo.xcprivacy -type f -print)"
          printf '%s\n' "$PRIVACY_MANIFESTS"
          PRIVACY_MANIFEST_COUNT="$(printf '%s\n' "$PRIVACY_MANIFESTS" | sed '/^$/d' | wc -l | tr -d ' ')"
          if [ "$PRIVACY_MANIFEST_COUNT" -eq 0 ]; then
            echo '::error::No PrivacyInfo.xcprivacy was packaged in the iPhone release'
            exit 1
          fi
          echo "Packaged privacy manifests: $PRIVACY_MANIFEST_COUNT"

      - name: Boot iPhone Simulator and launch Restaurant
        id: simulator
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build/ios-smoke

          RUNTIME=$(xcrun simctl list runtimes -j | python3 -c 'import json,sys; d=json.load(sys.stdin); a=[r for r in d["runtimes"] if r.get("isAvailable") and "iOS" in r.get("name","")]; a.sort(key=lambda r: tuple(int(x) for x in r.get("version","0").split("."))); print(a[-1]["identifier"])')
          DEVICE_TYPE=$(xcrun simctl list devicetypes -j | python3 -c 'import json,sys; a=[d for d in json.load(sys.stdin)["devicetypes"] if d.get("name","").startswith("iPhone")]; pref=["iPhone 17 Pro","iPhone 17","iPhone 16 Pro","iPhone 16"]; by={d["name"]:d for d in a}; pick=next((by[n] for n in pref if n in by),a[-1]); print(pick["identifier"])')

          UDID=$(xcrun simctl create "JETKIZ Restaurant CI" "$DEVICE_TYPE" "$RUNTIME")
          echo "udid=$UDID" >> "$GITHUB_OUTPUT"

          xcrun simctl boot "$UDID"
          xcrun simctl bootstatus "$UDID" -b
          xcrun simctl install "$UDID" build/ios/iphonesimulator/Runner.app
          xcrun simctl launch "$UDID" asia.jetkiz.restaurant | tee build/ios-smoke/launch.txt
          sleep 8

          xcrun simctl spawn "$UDID" launchctl list | grep -F 'asia.jetkiz.restaurant'
          xcrun simctl io "$UDID" screenshot build/ios-smoke/restaurant-login.png
          xcrun simctl spawn "$UDID" log show --style compact --last 2m --predicate 'process == "Runner"' > build/ios-smoke/runner.log || true
          test -s build/ios-smoke/restaurant-login.png
          if grep -Fq 'Firebase initialization failed' build/ios-smoke/runner.log; then
            echo 'Firebase bootstrap error detected in Restaurant simulator log' >&2
            grep -F 'Firebase initialization failed' build/ios-smoke/runner.log >&2 || true
            exit 1
          fi
          if grep -Fq '[core/not-initialized]' build/ios-smoke/runner.log; then
            echo 'Firebase core/not-initialized error detected in Restaurant simulator log' >&2
            grep -F '[core/not-initialized]' build/ios-smoke/runner.log >&2 || true
            exit 1
          fi
          echo 'Restaurant Firebase initialized without bootstrap errors'
          echo 'JETKIZ Restaurant launched and remained alive in iPhone Simulator'

      - name: Upload simulator evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: restaurant-ios-simulator-evidence
          path: |
            build/ios-smoke/restaurant-login.png
            build/ios-smoke/launch.txt
            build/ios-smoke/runner.log
          if-no-files-found: warn
          retention-days: 14

      - name: Shutdown simulator
        if: always() && steps.simulator.outputs.udid != ''
        shell: bash
        env:
          UDID: ${{ steps.simulator.outputs.udid }}
        run: |
          xcrun simctl shutdown "$UDID" || true
          xcrun simctl delete "$UDID" || true

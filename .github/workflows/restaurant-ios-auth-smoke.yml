name: Restaurant iOS Auth Smoke

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'lib/**'
      - 'ios/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - '.github/workflows/restaurant-ios-auth-smoke.yml'
  push:
    branches:
      - ios/restaurant-initial-release
    paths:
      - 'lib/**'
      - 'ios/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - '.github/workflows/restaurant-ios-auth-smoke.yml'

permissions:
  contents: read

concurrency:
  group: restaurant-ios-auth-smoke
  cancel-in-progress: true

jobs:
  ios-auth-smoke:
    runs-on: macos-26
    timeout-minutes: 35

    env:
      E2E_RESTAURANT_PHONE: ${{ secrets.E2E_RESTAURANT_PHONE }}
      E2E_RESTAURANT_PASSWORD: ${{ secrets.E2E_RESTAURANT_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Validate E2E credentials
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${E2E_RESTAURANT_PHONE:-}" ]; then
            echo "::error::Missing repository secret E2E_RESTAURANT_PHONE"
            exit 1
          fi
          if [ -z "${E2E_RESTAURANT_PASSWORD:-}" ]; then
            echo "::error::Missing repository secret E2E_RESTAURANT_PASSWORD"
            exit 1
          fi
          echo 'Restaurant E2E credentials are configured'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Environment
        shell: bash
        run: |
          set -euo pipefail
          flutter --version
          xcodebuild -version
          xcrun simctl list runtimes

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Boot clean iPhone Simulator
        id: simulator
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build/ios-auth-smoke
          RUNTIME=$(xcrun simctl list runtimes -j | python3 -c 'import json,sys; d=json.load(sys.stdin); a=[r for r in d["runtimes"] if r.get("isAvailable") and "iOS" in r.get("name","")]; a.sort(key=lambda r: tuple(int(x) for x in r.get("version","0").split("."))); print(a[-1]["identifier"])')
          DEVICE_TYPE=$(xcrun simctl list devicetypes -j | python3 -c 'import json,sys; a=[d for d in json.load(sys.stdin)["devicetypes"] if d.get("name","").startswith("iPhone")]; pref=["iPhone 17 Pro","iPhone 17","iPhone 16 Pro","iPhone 16"]; by={d["name"]:d for d in a}; pick=next((by[n] for n in pref if n in by),a[-1]); print(pick["identifier"])')
          UDID=$(xcrun simctl create "JETKIZ Restaurant Auth" "$DEVICE_TYPE" "$RUNTIME")
          echo "udid=$UDID" >> "$GITHUB_OUTPUT"
          xcrun simctl boot "$UDID"
          xcrun simctl bootstatus "$UDID" -b

      - name: Build and launch production Restaurant auth smoke
        timeout-minutes: 15
        shell: bash
        env:
          UDID: ${{ steps.simulator.outputs.udid }}
        run: |
          set -euo pipefail
          flutter build ios --simulator --debug --no-pub \
            --dart-define=JETKIZ_API_BASE_URL=https://api.jetkiz.asia \
            --dart-define=E2E_AUTH_SMOKE=true \
            --dart-define=E2E_RESTAURANT_PHONE="$E2E_RESTAURANT_PHONE" \
            --dart-define=E2E_RESTAURANT_PASSWORD="$E2E_RESTAURANT_PASSWORD"

          APP=build/ios/iphonesimulator/Runner.app
          LOG=build/ios-auth-smoke/runner.log
          test -d "$APP"

          xcrun simctl install "$UDID" "$APP"
          xcrun simctl launch "$UDID" asia.jetkiz.restaurant

          for attempt in $(seq 1 45); do
            xcrun simctl spawn "$UDID" log show --style compact --last 2m --predicate 'process == "Runner"' > "$LOG" || true
            if grep -Fq 'JETKIZ_RESTAURANT_E2E_AUTH_OK' "$LOG"; then
              echo 'Production Restaurant login smoke passed'
              exit 0
            fi
            if grep -Fq 'JETKIZ_RESTAURANT_E2E_AUTH_FAILED' "$LOG"; then
              grep -F 'JETKIZ_RESTAURANT_E2E_AUTH_FAILED' "$LOG" || true
              exit 1
            fi
            sleep 2
          done

          echo 'Timed out waiting for Restaurant auth smoke marker' >&2
          tail -200 "$LOG" || true
          exit 1

      - name: Capture simulator evidence
        if: always() && steps.simulator.outputs.udid != ''
        shell: bash
        env:
          UDID: ${{ steps.simulator.outputs.udid }}
        run: |
          mkdir -p build/ios-auth-smoke
          xcrun simctl io "$UDID" screenshot build/ios-auth-smoke/final-screen.png || true
          xcrun simctl spawn "$UDID" log show --style compact --last 10m --predicate 'process == "Runner"' > build/ios-auth-smoke/runner.log || true

      - name: Upload auth evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: restaurant-ios-auth-evidence
          path: |
            build/ios-auth-smoke/final-screen.png
            build/ios-auth-smoke/runner.log
          if-no-files-found: warn
          retention-days: 14

      - name: Shutdown simulator
        if: always() && steps.simulator.outputs.udid != ''
        shell: bash
        env:
          UDID: ${{ steps.simulator.outputs.udid }}
        run: |
          xcrun simctl shutdown "$UDID" || true
          xcrun simctl delete "$UDID" || true
